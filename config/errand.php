<?php

return [

    'pricing' => [
        // Базовая активация поручения (в minor units: øre / cent)
        'base_fee' => 5000, // 50 NOK, пример

        // Стоимость за километр маршрута (туда + обратно, если заложим)
        'distance_fee_per_km' => 800, // 8 NOK/км, пример

        // Стоимость за минуту ожидаемого времени выполнения
        'time_fee_per_minute' => 150, // 1.5 NOK/мин, пример

        // Коэффициенты сложности (множители)
        'complexity_multipliers' => [
            1 => 1.0,   // очень простое поручение
            2 => 1.1,
            3 => 1.25,
            4 => 1.5,
            5 => 1.8,   // очень сложное поручение
        ],

        // Надбавка за Trusted Helper (фиксированная)
        'trusted_helper_fee' => 10000, // 100 NOK, пример

        // Надбавка за срочность (множитель к (Base + Distance + Time + Complexity))
        'urgency_multiplier' => 1.25, // +25% при срочности

        // Верхний и нижний лимиты для итоговой оценки (страховочные, можно не использовать жестко)
        'min_total_estimated_price' => 5000,
        'max_total_estimated_price' => 200000,
    ],

];
